import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        width: 'auto',
        height: 'auto',
        marginVertical: 20
    }
})

export default styles;