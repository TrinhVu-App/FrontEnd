import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        width: 60,
        height: 175,
        // borderWidth: 2,
        // borderColor: "#11C4EA",
        backgroundColor: "#11C4EA",
        justifyContent: 'space-evenly',
        borderRadius: 50,
        alignItems: 'center'
    }
})

export default styles;